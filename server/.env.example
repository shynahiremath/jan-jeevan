PORT=5000
# Optional — required for auth + finance dashboard
MONGO_URI=mongodb://127.0.0.1:27017/jan-jeevan
# Required for auth tokens
JWT_SECRET=change-me-to-a-long-random-string
# Optional — data.gov.in API key for live mandi prices
MANDI_API_KEY=
